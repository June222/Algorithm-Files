### Dynamic Programming 적용 버전

# 22-07-21-02:36
현재 Problem에 대한 특정 숫자에 대한 출력은 그보다 작은 숫자들의 합으로 이루어진다.

ex) F(4) =	F(3)가 가진 모든 경우에 (+ 1)만 뒤에 붙여 놓은 것 +
			F(2)가 가진 모든 경우에 (+ 2)만 뒤에 붙여 놓은 것 +
			F(1)가 가진 모든 경우에 (+ 3)만 뒤에 붙여 놓은 것 
			으로 이루어지기 때문에, DP로 해결이 가능하다.